import styled from 'styled-components';

const Title = styled.div`
  font-size: 6rem;
  font-weight: bold;
  letter-spacing: .1rem;
  text-align: center;
  color: var(--paleBlue);
`;

export default Title;
